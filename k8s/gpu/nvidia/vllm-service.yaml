apiVersion: v1
kind: Service
metadata:
  name: vllm
  namespace: llm-ops
  labels:
    app: vllm
    app.kubernetes.io/name: vllm
    app.kubernetes.io/part-of: llm-ops-platform
spec:
  type: NodePort
  selector:
    app: vllm
  ports:
    - name: http
      port: 8000
      targetPort: http
      nodePort: 30081
      protocol: TCP
